@model HomeVM
@{
    ViewData["Title"] = "Home Page";
}

<div class="container">
    @* Categories *@
    <div class="d-flex justify-content-center">
        <button class="btn btn-success mx-2">Всі категорії</button>
        @foreach(var category in Model.Categories)
        {
            <button class="btn btn-success mx-2">@category.Name</button>
        }
    </div>

    @* Products *@
    <div class="row">
        <div class="container mt-5">
            <div class="row">
                @foreach (var product in Model.Products)
                {
                    <div class="card m-2 p-2" style="width: 18rem;">
                        <img src="@product.Image" class="card-img-top" alt="@product.Name">
                        <div class="card-body">
                            <h5 class="card-title">@product.Name</h5>
                            <p class="card-text">@product.Description</p>
                            <p class="card-text fw-bold" style="font-size: 1.5em;">@product.Price грн</p>
                            <a href="#" class="btn btn-primary">Додати у кошик</a>
                        </div>
                    </div>
                }
            </div>
        </div>
    </div>
</div>